Задача реализовать одностраничное приложение с формой,
в которую можно загрузить json файл (в том числе с использованием drag and drop).
Если формат json верный, то нужно отобразить число объектов, если нет – сообщение об ошибке.